import { OrdinalPipePipe } from './ordinal-pipe.pipe';

describe('OrdinalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OrdinalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
